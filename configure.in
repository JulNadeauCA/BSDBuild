# $Csoft: configure.in,v 1.3 2002/05/13 07:42:32 vedge Exp $

makeout(Makefile.config);

require(make);

if [ "${version}" = "yes" ]; then
	version();
fi
if [ "${help}" = "yes" ]; then
	help();
fi

mdefine(SHAREDIR, "$PREFIX/share/csoft-mk");

