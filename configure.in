# $Csoft: configure.in,v 1.6 2003/05/05 11:00:33 vedge Exp $

makeout(Makefile.config);
inclout(config);
logout(config.log);

if [ "${help}" = "yes" ]; then
	help();
fi

mdefine(SHAREDIR, "$PREFIX/share/csoft-mk");

