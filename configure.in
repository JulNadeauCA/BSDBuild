# $Csoft: configure.in,v 1.1 2002/05/10 23:07:36 vedge Exp $

makeout(Makefile.config);

if ("${version}" = "yes") {
	version();
};
if ("${help}" = "yes") {
	help();
};

mdefine(SHAREDIR, "$PREFIX/share/csoft-mk");

