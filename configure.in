# $Csoft: configure.in,v 1.2 2002/05/13 07:41:36 vedge Exp $

makeout(Makefile.config);

if [ "${version}" = "yes" ]; then
	version();
fi
if [ "${help}" = "yes" ]; then
	help();
fi

mdefine(SHAREDIR, "$PREFIX/share/csoft-mk");

