# $Csoft: configure.in,v 1.4 2002/05/29 21:32:10 vedge Exp $

makeout(Makefile.config);

if [ "${version}" = "yes" ]; then
	version();
fi
if [ "${help}" = "yes" ]; then
	help();
fi

mdefine(SHAREDIR, "$PREFIX/share/csoft-mk");

